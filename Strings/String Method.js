// Trim method in js

let str = "       help!";
let newstr = str.trim().toUpperCase()
console.log("After update: ",newstr);

// replace method

let str1 = "ILOVECODING";
let newstr1 = str1.slice(0).replace("L","T");
console.log(newstr1);
 
// trim method

let msg = "   hello   ";
let newmsg = msg.trim()
console.log("the trim example: ",newmsg);

// Channing MeTHOD

let msg1 = "    ILOVECODING     ";
let newmsg1 = msg.trim().toLowerCase()
console.log("the Upper and lowercase example: ",newmsg1);