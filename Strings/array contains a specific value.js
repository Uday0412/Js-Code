function containsValue(arr, value) {
    return arr.includes(value);
}

// Example usage:
const arr = [10, 20, 30, 40];
console.log(containsValue(arr, 30));  // Output: true
console.log(containsValue(arr, 50));  // Output: false
